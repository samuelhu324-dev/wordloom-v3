# DEVTEST-DB-5435 (LOCAL DEV)
#
# Use with local Python / alembic / uvicorn (NOT for docker backend container).
# This points to the dev database on localhost:5435.
#
DATABASE_URL=postgresql+psycopg://wordloom:wordloom@localhost:5435/wordloom_dev

DEBUG=True
ENVIRONMENT=devtest-dev
LOG_LEVEL=INFO
