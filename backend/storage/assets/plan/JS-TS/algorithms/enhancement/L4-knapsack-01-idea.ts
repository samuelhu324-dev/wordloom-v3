// L4. 0/1 Knapsack (idea + template)
// L4. 0/1 背包（思路 + 模板）

// Given n items with weights w[i] and values v[i], and a backpack capacity W,
// find the maximum total value you can take where each item can be taken 0 or 1 time.
// 有 n 件物品，每件物品重量 w[i]、价值 v[i]，背包容量为 W，每件物品只能取 0 或 1 次，求能获得的最大总价值。

export function knapSack01(weights: number[], values: number[], capacity: number): number {
  // TODO: implement (DP: 2D or 1D rolling array)
  return 0;
}
