# Docker-only environment variables for backend
# Backend container connects to the Postgres service in docker-compose
DATABASE_URL=postgresql+psycopg://wordloom:wordloom@db:5432/wordloom

# Match backend/.env keys (optional)
DEBUG=False
ENVIRONMENT=docker
LOG_LEVEL=INFO

# SECRET_KEY=replace-me
