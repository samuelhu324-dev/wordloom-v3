# Recommended .env.local for WordloomFrontend/next
NEXT_PUBLIC_API_BASE=http://localhost:8000/api/common
NEXT_PUBLIC_SOURCES_ENDPOINT=/sources/
NEXT_PUBLIC_SEARCH_ENDPOINT=/entries/search
# Optional overrides (leave commented unless you need them)
# NEXT_PUBLIC_INSERT_ENDPOINT=/entries/batch
# NEXT_PUBLIC_RENAME_ENDPOINT=/sources/rename
# NEXT_PUBLIC_RECENT_ENDPOINT=/entries/recent
