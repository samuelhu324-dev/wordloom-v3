# Recommended .env.local for WordloomFrontend/next
# 默认使用相对路径 + Next 重写（见 next.config.mjs 的 rewrites）；不强制直连端口。
# 如需强制直连绝对地址，可设置 NEXT_PUBLIC_FORCE_ABSOLUTE_API=1 并填写下面两个 BASE 变量。

# 0=使用相对路径（推荐），1=强制使用绝对地址
NEXT_PUBLIC_FORCE_ABSOLUTE_API=0

# 仅在 FORCE_ABSOLUTE_API=1 时生效
NEXT_PUBLIC_LOOM_API_BASE=
NEXT_PUBLIC_ORBIT_API_BASE=

# Orbit 静态资产前缀（用于把相对路径拼成可访问 URL）
NEXT_PUBLIC_ORBIT_ASSET_PREFIX=/orbit-assets

# 后端 URL（用于加载媒体文件等）
NEXT_PUBLIC_BACKEND_URL=http://localhost:8000

# Next 开发代理目标（被 next.config.mjs 读取）
LOOM_DEV_TARGET=http://localhost:8013
ORBIT_DEV_TARGET=http://localhost:8000

