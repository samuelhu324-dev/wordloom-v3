// K4. Jump Game
// K4. 跳跃游戏

// Given an array of non-negative integers nums, where nums[i] is the maximum
// jump length from index i, determine if you can reach the last index.
// 给定非负整数数组 nums，nums[i] 表示在下标 i 处最多能跳多远，判断是否能跳到最后一个下标。

export function canJump(nums: number[]): boolean {
  // TODO: implement
  return false;
}
