// H3. 判断链表是否有环（快慢指针）
// H3. Linked list cycle detection (fast & slow pointers)

// 给定一个单链表的头节点 head，判断链表中是否存在环。
// 返回 true 表示存在环，false 表示不存在。
// Given the head of a singly linked list, determine if there is a cycle in the list.
// Return true if there is a cycle, otherwise false.

export interface ListNode {
    val: number;
    next: ListNode | null;
}

export function hasCycle(head: ListNode | null): boolean {
    // TODO: implement fast & slow pointer detection
    return false;
}
