// K3. Meeting Rooms / Meeting Rooms II
// K3. 会议室 / 会议室数量

// Variant 1: Given meeting time intervals, determine if a person could attend all meetings.
// 变体1：给定若干会议时间区间，判断是否能参加所有会议（区间是否有重叠）。

export function canAttendMeetings(intervals: Array<[number, number]>): boolean {
  // TODO: implement
  return false;
}

// Variant 2: Given meeting intervals, return the minimum number of conference rooms required.
// 变体2：给定会议区间，返回所需的最少会议室数量。

export function minMeetingRooms(intervals: Array<[number, number]>): number {
  // TODO: implement
  return 0;
}
