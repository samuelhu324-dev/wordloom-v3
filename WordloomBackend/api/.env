# 运行环境
APP_ENV=dev

# 项目根（可选，不设也行，path_manager 会自动识别）
WORDLOOM_ROOT=D:\Project\Wordloom

# ——数据库位置：唯一真源（只维护这两项）——
BACKEND_DB=D:\Project\Wordloom\WordloomBackend\api\storage\app.db
FRONTEND_DB=D:\Project\Wordloom\WordloomFrontend\streamlit\app.db

# 兼容旧代码（可留可删；建议在代码里由 BACKEND_DB 统一构建）
DATABASE_URL=sqlite:///D:/Project/Wordloom/WordloomBackend/api/storage/app.db

# 开发期固定登录（仅本地调试，生产必须移除或改为真正的鉴权）
APP_USER=admin
APP_PASS=admin123
APP_TOKEN=devtoken123

# 调试跨域（Streamlit）
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

DISABLE_AUTH=1
