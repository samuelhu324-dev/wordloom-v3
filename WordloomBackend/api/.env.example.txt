# Minimal, clean environment for Wordloom Backend
APP_ENV=dev
ORBIT_ENABLED=false
# Use psycopg v3 driver, host is the Docker-published port 5444, database is wordloompg
DATABASE_URL=postgresql+psycopg://postgres:pgpass@localhost:5444/wordloompg
