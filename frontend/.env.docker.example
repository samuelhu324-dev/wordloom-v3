# Docker-only environment variables for frontend
NEXT_PUBLIC_USE_MOCK=0
NEXT_PUBLIC_API_BASE=http://localhost:31001
NEXT_PUBLIC_API_PREFIX=/api/v1

# Note:
# - NEXT_PUBLIC_* variables are used by the browser.
# - If you add server-side fetches in Next.js, you may want a separate (non-public)
#   internal base URL for container-to-container calls.
