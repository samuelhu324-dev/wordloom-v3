# Frontend local env
# Browser hits frontend on 3000
# Browser axios hits backend directly on 30001

# Use real backend (disable mocks)
NEXT_PUBLIC_USE_MOCK=0

# Direct backend API base (axios will hit this directly from browser)
# Windows 浏览器访问 WSL2 服务更稳定：使用 WSL2 IP:30001
NEXT_PUBLIC_API_BASE=http://localhost:30001

# API prefix to use (match backend at /api/v1)
NEXT_PUBLIC_API_PREFIX=/api/v1
