# Frontend local env for same-origin proxy (30001)
# Browser always hits http://localhost:30001
# Next.js rewrites will proxy /api prefix to backend target (API_PROXY_TARGET)

# Use real backend (disable mocks)
NEXT_PUBLIC_USE_MOCK=0

# Same-origin base so axios hits Next first
NEXT_PUBLIC_API_BASE=http://localhost:30001

# Rewrites configuration (server-side only)
# Backend FastAPI server base
API_PROXY_TARGET=http://localhost:30002
# API prefix to use (match backend). Set to /api/v1 or /api accordingly.
API_PROXY_PREFIX=/api/v1
