# Frontend local development env template (personal override)
#
# 推荐用法：不要把 dev/test/sandbox 都塞进一个 .env.local 里来回改。
# 直接用 npm scripts 选环境：
#   - npm run dev:dev
#   - npm run dev:test
#   - npm run dev:sandbox
#
# 或者参考这些示例文件：
#   - .env.dev.local.example
#   - .env.test.local.example
#   - .env.sandbox.local.example
NEXT_PUBLIC_USE_MOCK=0
# Backend API origin (dev: 30001, test: 30011)
NEXT_PUBLIC_API_BASE=http://localhost:30001
NEXT_PUBLIC_API_PREFIX=/api/v1

# Next.js rewrites proxy target (should match NEXT_PUBLIC_API_BASE)
API_PROXY_TARGET=http://localhost:30001
